let playPause = function playPauseTimer(btn,hour,mins) {

}

let init = function initTimer(playPause, hour, mins) {
  playPause.addEventListener('click', () => {
    console.log("playPause Clicked!");
  });
}

module.exports = {
  init
}
